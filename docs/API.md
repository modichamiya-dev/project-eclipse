# Eclipse API 1.0

Consumers depend on the `api` module and obtain capabilities through `EclipseApi.service(Class)`. Public contracts are semantic-versioned. Implementations and relocated libraries are not API. Breaking changes require a new major version; deprecated contracts remain for at least one minor release.
