# Eclipse asset sources

Place author-friendly source assets under `assets/eclipse/` and register each file in `assets.yml` with a stable namespaced key. Generated ZIP output is never edited or committed.
